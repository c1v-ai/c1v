# module-5-formfunction (reserved slot)

**Status:** Empty scaffold. Populated by Wave 3 team `c1v-m5-formfunction` per `plans/c1v-MIT-Crawley-Cornell.v2.md` §0.3 / §15 (T5).

## Context

This slot holds Crawley-style **form–function** analysis (object-process mapping, allocation of function to form). It's a NEW insertion in the hybrid MIT-Crawley-Cornell sequence, sitting between:

- **M4 decision-matrix** (upstream — concept selection)
- **M6 QFD/HoQ** (downstream — customer-to-engineering mapping)

Previously the slot was occupied by QFD in Cornell-linear numbering. The hybrid renumber (v2 §0.4) moved QFD to M6 and reserved M5 for form-function.

## Expected artifacts (when populated)

- `form_function_mapping.json` — function → form allocation per Crawley chs. 7-9
- `object_process_diagram.mmd` — OPM-style system view
- `final_report.md` — narrative tying to M3 FFBD upstream and M6 QFD downstream
- `validation_report.json` — schema + referential checks

## Do not edit until dispatched

T5 team owns this directory. Touching it before dispatch risks dependency collisions with Wave 3.
