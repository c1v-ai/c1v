/**
 * Submodule 3.2 — Flows + Branching
 *
 * Re-exports the shortcuts/reference-blocks schema per
 * `plans/reorg-mapping.md` §3. Gate C functional-flow phases (flows +
 * branching + decomposition — pending) should register against this
 * submodule.
 *
 * Source: `phase-6-shortcuts-reference-blocks.ts` (registry slug preserved).
 *
 * @module lib/langchain/schemas/module-3/submodule-3-2-flows-branching
 */

export {
  phase6Schema,
  type Phase6Artifact,
  arrowShortcutSchema,
  type ArrowShortcut,
  referenceBlockSchema,
  type ReferenceBlock,
} from './phase-6-shortcuts-reference-blocks';
