---
phase: 02-observability-wiring
plan: 01
wave: 1
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - plans/v21-outputs/observability/sentry-baseline-2026-04-27.json
---

# Plan 02-01: Verify OBS-01/02 Already Wired; Update Tracking; Document OBS-03 Gate

## Objective

Wave E already shipped `setSentryTransport` boot wiring and `recordSynthesisMetricsTotal` emission on the LLM-only path. The REQUIREMENTS/ROADMAP still say "not done." This plan verifies both are present in source, marks them complete, updates the baseline JSON status, and documents the OBS-03 human gate.

## Tasks

### Task 1: Grep-verify OBS-01
Confirm `setSentryTransport` is called in `instrumentation.ts` (not just defined in synthesis-metrics.ts).

### Task 2: Grep-verify OBS-02
Confirm `recordSynthesisMetricsTotal` with `impl: 'llm-only'` fires in `generate-nfr.ts` and `generate-constants.ts`, and that `nfrImpl` defaults to `'llm'` in `createIntakeGraph`.

### Task 3: Update sentry-baseline-2026-04-27.json
Change `status` from `gap_surfaced` → `wired_awaiting_traffic`. Update `next_action` to reflect that wiring is live and the only remaining step is the 48h traffic accumulation gate.

### Task 4: Mark OBS-01 and OBS-02 complete in REQUIREMENTS.md
Flip `[ ]` → `[x]` for OBS-01 and OBS-02 with evidence references.

### Task 5: Update ROADMAP.md
Correct the stale Phase 2 description. OBS-01/02 done; OBS-03 is a time-gated human action after ≥48h prod traffic.

### Task 6: Update STATE.md
Mark Phase 2 in-progress (OBS-03 pending human gate).

## Success Criteria
- [ ] OBS-01 confirmed in instrumentation.ts via grep
- [ ] OBS-02 confirmed in generate-nfr.ts + generate-constants.ts via grep
- [ ] sentry-baseline status = wired_awaiting_traffic
- [ ] REQUIREMENTS.md OBS-01 + OBS-02 marked [x]
- [ ] ROADMAP.md Phase 2 reflects accurate state
