# Artifact generator Python deps (pinned per v2 §15.7 extender guardrail)
# Install:  pip install -r scripts/artifact-generators/requirements.txt
jsonschema==4.23.0
openpyxl==3.1.5
python-pptx==1.0.2
matplotlib==3.9.2
networkx==3.3
graphviz==0.20.3
# PDF rendering for gen-arch-recommendation — owned by extender, listed
# here so the single requirements.txt remains authoritative.
# weasyprint==62.3   # uncomment when extender lands gen-arch-recommendation
