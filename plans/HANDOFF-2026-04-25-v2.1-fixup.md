# HANDOFF — v2.1 spawn-prompts fix-up sweep

> **Date:** 2026-04-25 19:50 EDT
> **Author:** Bond
> **Status:** PENDING — fresh Claude session executes the fix-up per this doc
> **Trigger:** David approved the fix-up plan at 19:50 EDT then cleared context for fresh execution

---

## TL;DR

`.claude/plans/team-spawn-prompts-v2.1.md` (1153 lines, 5 teams, 25 agents) is **DRAFT** and needs a fix-up sweep applying ~21 corrections from the critique at `.claude/plans/team-spawn-prompts-v2.1-CRITIQUE.md`. David locked all rulings; fresh Claude executes mechanically.

**One master-plan edit already shipped this session:** `D-V21.24` (Vercel ↔ Cloud Run boundary lock) added to `plans/c1v-MIT-Crawley-Cornell.v2.1.md` at the bottom of the Decisions table.

---

## David's rulings (locked 2026-04-25 19:50 EDT — verbatim)

```
#22 - ship the dispatch helper.ts  (R-v2.1.A) - C) 14 (R-v2.1.D) confirm
```

Decoded:
1. **Critique Issue #22** — ship `scripts/dispatch-helper.ts` before fanning out 25 agents (process hygiene; pins exact `Skill()` injection text + prevents per-agent prompt drift).
2. **R-v2.1.A — Option C** — keep TA1 5-7 day timeline target with explicit fail-forward: any agent fs-side-effects refactor > 200 LOC ships as a graph-node-adapter wrapper in v2.1 + the underlying refactor defers to v2.2 day 0.
3. **R-v2.1.D — Confirm** — `≤ $500/mo at 100 DAU` is the v2.1 ship gate. AV.01 portfolio-keystone alignment ($320/mo) carries to v2.2 (Wave E heuristic-engine cost lever). Free-tier hard-cap (1 synthesis/mo) is the structural backstop during the Wave-A unoptimized window.

**Implicit approvals (Group A factual + Group B Bond-default architectural locks):** all approved by virtue of "yes go ahead" + David didn't flip any default in the triage table.

---

## Files in scope

| File | Status | Action |
|---|---|---|
| `plans/c1v-MIT-Crawley-Cornell.v2.1.md` | ✅ D-V21.24 added this session | Verify line ≈236 contains the new D-V21.24 row |
| `.claude/plans/team-spawn-prompts-v2.1.md` | ❌ DRAFT pending fix-up | Apply 21 edits below |
| `.claude/plans/team-spawn-prompts-v2.1-CRITIQUE.md` | ✅ Reference only | Source of all 22 issues; #22 + Issues 7+14 = David's rulings |
| `scripts/dispatch-helper.ts` | ❌ NEW — not yet built | Build per spec in §"dispatch-helper.ts spec" below |
| `apps/product-helper/CLAUDE.md` | 🔒 Pending David authorization (file-safety rule) | Surface diff in `plans/v21-outputs/ta1/claude-md-diff.md` FIRST; do NOT directly edit |

---

## Per-issue fix specifications

Numbering matches `.claude/plans/team-spawn-prompts-v2.1-CRITIQUE.md` Table of Contents.

### Group A — Factual corrections (apply directly)

#### Issue 1 + 2 + 20 — METHODOLOGY-CORRECTION canonical path
**Reality:** Only `.claude/plans/kb-upgrade-v2/METHODOLOGY-CORRECTION.md` and `plans/kb-upgrade-v2/METHODOLOGY-CORRECTION.md` exist. There is NO `system-design/METHODOLOGY-CORRECTION.md`. `plans/kb-upgrade-v2/` contains modules 1-8 (full); `.claude/plans/kb-upgrade-v2/` contains only modules 1-6.

**Fix:**
- TA1 §`canonical_paths_to_verify` line ≈86: replace `system-design/METHODOLOGY-CORRECTION.md` with `plans/kb-upgrade-v2/METHODOLOGY-CORRECTION.md`. Bond default = pick `plans/kb-upgrade-v2/` (only path with all 8 modules).
- TA1.migrations-and-agent-audit deliverable ≈ line 119: change `apps/product-helper/CLAUDE.md` rewrite target from `.claude/plans/kb-upgrade-v2/module-{1..8}/` → `plans/kb-upgrade-v2/module-{1..8}/`.
- Same agent's "Resolve canonical METHODOLOGY-CORRECTION.md home" deliverable: replace the OR clause; lock to `plans/kb-upgrade-v2/` as canonical home; convert `.claude/plans/kb-upgrade-v2/METHODOLOGY-CORRECTION.md` to a one-line redirect stub.
- Dispatch blockers §"Still open" line ≈950: collapse R-v2.1.E to RESOLVED (no decision needed); reduce open-ruling count from 5 → 4.

#### Issue 6 — M2 NFR agent location handwaved
**Reality:** Verified at `apps/product-helper/lib/langchain/agents/system-design/nfr-resynth-agent.ts`.

**Fix:** TA1.open-questions-emitter deliverable ≈ line 197 — replace "Existing M2 NFR agent (location TBD by `migrations-and-agent-audit` audit; likely lib/langchain/agents/extraction-agent.ts or similar)" with the verified path + emit pattern (see critique line 188-194 for the replacement copy).

#### Issue 13 — Manifest contract version
**Fix:** TA3.docs deliverable ≈ line 571 — change `manifest_contract_version: 'v2.1.1'` → `'v1'`. Aligns with `nfr_engine_contract_version: 'v1'` envelope convention. Document version-bump rule (major on shape break, minor on additive fields) in `plans/v21-outputs/ta3/manifest-contract.md`.

#### Issue 15 — `inputs_hash` already exists
**Reality:** Verified shipped in v2:
- `apps/product-helper/lib/langchain/agents/architecture-recommendation-agent.ts:605` — `inputs_hash: inputsHash(mods)`
- `apps/product-helper/lib/langchain/agents/system-design/synthesis-agent.ts:420`
- `apps/product-helper/lib/langchain/schemas/synthesis/architecture-recommendation.ts:745` — Zod regex `^[0-9a-f]{64}$/u`

**Fix:** TB1.cache-and-lazy-gen ≈ line 630-634 — replace "build inputs_hash" framing with "consume existing inputsHash() helper":

```
synthesis-cache.ts:
  - Compute new project's inputs_hash via existing inputsHash(mods) helper (sha256 of canonical intake payload + upstream module shas)
  - SELECT * FROM project_artifacts WHERE inputs_hash = $1 AND synthesis_status = 'ready' LIMIT 1
  - On hit: COPY cached storage_path into new project's row (no blob duplication); mark synthesis_status='ready'
  - On miss: fall through to normal generation
References:
  - lib/langchain/agents/architecture-recommendation-agent.ts:605
  - lib/langchain/schemas/synthesis/architecture-recommendation.ts:745
```

#### Issue 17 — api-spec-agent line numbers
**Reality:** `apiSpecificationSchema` is at line 127, not 71. Line 71 is `jsonSchemaSchema`.

**Fix:**
- TD1.preflight-and-stage1-schema guardrail ≈ line 794: `api-spec-agent.ts:71 apiSpecificationSchema` → `api-spec-agent.ts:127 apiSpecificationSchema`
- TD1.stage2-deterministic-expansion guardrail ≈ line 820: same fix
- Keep TD1 §diagnosis line 763 reference `:71-127` as-is (it correctly describes the embedded jsonSchemaSchema range)

#### Issue 18 — Fixture-only preflight may mask real bug
**Fix:** TD1.preflight-and-stage1-schema goal + deliverables — capture both fixture AND live preflight (~$0.20 cost trivial vs mis-branching cost):

```
Preflight steps (in order):
  1. Fixture replay locally — capture stop_reason + usage. Commit to plans/v21-outputs/td1/preflight-log-fixture.md
  2. Live re-run against Anthropic API (production model ID + temp + maxTokens) — capture stop_reason + usage. Commit to plans/v21-outputs/td1/preflight-log-live.md
  3. If divergent: pick LIVE branch decision (production = reality); document divergence as known fixture-vs-live drift
  4. If they agree: proceed with shared branch decision
```

#### Issue 19 — 300s polling on prompt-cache TTL boundary
**Fix:** Wave gate procedure ≈ line 885 — change `300 seconds` → `270 seconds` (cache stays warm; 13 polls/hr). Optional: back off to 1200s after 30min idle (one cache miss buys 20+ min).

### Group B — Architectural locks (Bond defaults; David approved)

#### Issue 3 + 21 — Agent-disposition table
**Reality:** 4 of the 7 GENERATE_* nodes already exist at `apps/product-helper/lib/langchain/graphs/intake-graph.ts:388-391`:

```ts
.addNode('generate_ffbd', generateFFBD)
.addNode('generate_decision_matrix', generateDecisionMatrix)
.addNode('generate_qfd', generateQFD)
.addNode('generate_interfaces', generateInterfaces)
```

Plus on-disk system-design agents not addressed in current draft: `data-flows-agent.ts`, `nfr-resynth-agent.ts`, `n2-agent.ts`, `synthesis-agent.ts` (sibling of `architecture-recommendation-agent.ts`).

**Fix:** Replace TA1.langgraph-wirer goal + first deliverable with this **agent ↔ graph-node disposition table**:

| Agent file | Disposition | Graph node |
|---|---|---|
| `data-flows-agent.ts` | WIRE (NEW) | `generate_data_flows` |
| `nfr-resynth-agent.ts` | RE-WIRE existing extract_data → AUGMENT | (existing extract_data node augmented) |
| `ffbd-agent.ts` | already wired | `generate_ffbd` |
| `form-function-agent.ts` | WIRE (NEW) | `generate_form_function` |
| `decision-net-agent.ts` | WIRE (NEW — sibling to existing `generate_decision_matrix` per Issue 4) | `generate_decision_network` |
| `n2-agent.ts` | WIRE (NEW) | `generate_n2` |
| `interface-specs-agent.ts` | RE-WIRE (replace `generate_interfaces` internals) | `generate_interfaces` |
| `hoq-agent.ts` | RE-WIRE (replace `generate_qfd` internals) | `generate_qfd` |
| `fmea-early-agent.ts` | WIRE (NEW) | `generate_fmea_early` |
| `fmea-residual-agent.ts` | WIRE (NEW) | `generate_fmea_residual` |
| `synthesis-agent.ts` | OUT-OF-SCOPE (sibling of architecture-recommendation-agent; pick ONE — pick `architecture-recommendation-agent.ts` as keystone since it's 644 LOC + has tests) | n/a |
| `architecture-recommendation-agent.ts` | WIRE (NEW keystone) | `generate_synthesis` |
| `discriminator-intake-agent.ts`, `signup-signals-agent.ts` | M0 (T7) — OUT-OF-SCOPE for v2.1 | n/a |

Goal becomes: "AUGMENT 3 existing graph nodes (decision-matrix→decision-net invocation, qfd→hoq invocation, interfaces→interface-specs invocation), ADD 6 NEW graph nodes (form_function, decision_network, n2, fmea_early, fmea_residual, synthesis, data_flows), RE-WIRE existing extract_data node to call nfr-resynth-agent for the M2 NFR slice."

(Net: 3 augment + 6 add + 1 re-wire + 1 keystone = 11 graph-node touches. Not "7 new".)

#### Issue 4 — Decision Matrix vs Decision Network coexistence
**Lock:** Option A — two graph nodes coexist:
- `generate_decision_matrix` (existing, unchanged) → drives FROZEN `decision-matrix-viewer.tsx` (Cornell weighted-scoring view)
- `generate_decision_network` (NEW, invokes `decision-net-agent.ts`) → drives new viewer that TA2 ships

Both write to `project_artifacts` with `kind='decision_matrix_v1'` vs `kind='decision_network_v1'`. Add to TA1.langgraph-wirer guardrails.

#### Issue 5 — fs-side-effects refactor scope creep + R-v2.1.A Option C fail-forward
**Lock:** Add to TA1.langgraph-wirer guardrails:

```
- If audit surfaces an agent with > 200 LOC fs-side-effects refactor needed,
  langgraph-wirer ships a graph-node-adapter WRAPPER (NOT a refactor):
  - Adapter pattern: graph-node receives agent's existing fs-emit output
    via stdout capture + parses; persistence happens at graph-node layer
  - Documented JSDoc on the adapter pattern
- The deferred underlying refactor is appended as TODO in agent file +
  recorded in plans/post-v2.1-followups.md (for v2.2 day 0 pickup)
- Wave A does NOT block on the >200-LOC refactor decision
- This is the explicit fail-forward semantic for R-v2.1.A Option C
  (David ruling 2026-04-25 19:50 EDT)
```

#### Issue 8 — `fmea-viewer.tsx` FROZEN status
**Lock:** Mark **MODIFIABLE-IN-V21** (added by T10/T6 in v2 cycle; not at FROZEN bar yet).

**Fix:**
- TA2 §`ui_freeze_v2_carryover` line ≈279: explicitly enumerate FROZEN list as `decision-matrix-viewer.tsx, ffbd-viewer.tsx, qfd-viewer.tsx, interfaces-viewer.tsx, diagram-viewer.tsx` (5 total); list `fmea-viewer.tsx` as MODIFIABLE-IN-V21 (TA2.nav-and-pages line 336 already implies edit).
- TA2.verifier auto-FAIL list excludes `fmea-viewer.tsx`.

#### Issue 9 — DBML transpiler dependency
**Lock:** `@dbml/core` (NPM, MIT-licensed).

**Fix:** TA2.architecture-and-database deliverable ≈ line 360 — replace open-ended choice with locked dependency:

```
apps/product-helper/lib/db/dbml-transpiler.ts — pure-TS transpiler from internal schema shape (Drizzle-style entities + relations) to DBML syntax.
Dependency (locked): @dbml/core (NPM, MIT-licensed). Use the programmatic API.
Supported subset (locked):
  - Tables with primary keys, foreign keys (single-column)
  - Composite PKs
  - Enum types
  - Unique indexes
  - Optional FK relationships
NOT SUPPORTED in v2.1: views, stored procedures, partitions, complex indexes (functional, partial). Unsupported construct → emit DBML comment with source SQL inline.
Round-trip tested against fixture schemas at __tests__/fixtures/dbml/.
```

#### Issue 10 — Brand-token regex semantics
**Fix:** TA2.verifier ≈ line 420 — restate as positive allowlist:

```
Brand-token compliance regex check:
  Run: rg -n '#[0-9A-Fa-f]{6}' --type ts --type tsx <diff-paths>
  ALLOWED hits: file ∈ {app/theme.css, app/globals.css}, OR enclosed in /* */ or // comment, OR in *.test.{ts,tsx} fixture string.
  FAIL on any hit outside the allowlist.
```

#### Issue 11 — Sidecar vs Vercel LangGraph boundary
**Lock:** Per **D-V21.24** (already added to master plan v2.1 this session): Vercel hosts LangGraph orchestration; Cloud Run sidecar = pure rendering.

**Fix:**
- TA3.python-sidecar goal ≈ line 492 — replace "(or invokes the Vercel-side LangGraph and just renders results — pick: recommend ...)" with definitive lock per D-V21.24: "Sidecar receives `POST /run-render` with `{project_id, artifact_kind, agent_output_payload}`; renders via canonical Python generators; writes `project_artifacts` row. NO LangGraph orchestration on sidecar — that lives on Vercel side per D-V21.24."
- TA3 context add `D-V21.24` reference to authoritative_spec.

#### Issue 12 + 16 — Free-tier flag mechanism
**Lock:** Env var `SYNTHESIS_FREE_TIER_GATE = 'log_only' | 'enabled' | 'disabled'`. TA3 ships pre-stub at `'log_only'` (records the would-be 402 in Sentry without blocking). TB1 flips default to `'enabled'` as part of `tb1-wave-b-complete` tag.

**Fix:**
- TA3.synthesis-api-routes ≈ line 532 — replace open OR clause: "TA3 pre-stubs the tier check via `checkSynthesisAllowance` import + invocation in `/api/projects/[id]/synthesize/route.ts`. Wave A implementation: stub returns `{allowed: true}` if `SYNTHESIS_FREE_TIER_GATE !== 'enabled'`; logs the would-be 402 to Sentry when `SYNTHESIS_FREE_TIER_GATE === 'log_only'` (default). Wave B replaces stub with real DB-backed implementation."
- TB1.tier-and-circuit-breaker ≈ line 654 — pin: "Real DB-backed `checkSynthesisAllowance` in `lib/billing/synthesis-tier.ts`. Flip env var default `SYNTHESIS_FREE_TIER_GATE='enabled'` as part of `tb1-wave-b-complete` tag commit. Document in `apps/product-helper/.env.example`."

### Group C — Master plan v2.1 edit (already shipped this session)

✅ **D-V21.24** added to `plans/c1v-MIT-Crawley-Cornell.v2.1.md` Decisions table (right after D-V21.23, before the `---` separator). Verify the row exists at approximately line 236.

### Group D — Process hygiene (David approved per "#22")

#### Build `scripts/dispatch-helper.ts` BEFORE Dispatch Wave 1

**Path:** `scripts/dispatch-helper.ts` (repo root, mirrors `scripts/verify-tree-pair-consistency.ts` placement)

**Spec:**

```
Purpose: pin the exact text of the Skill-injection block prepended to every Agent({prompt: ...})
         body at dispatch time. Prevents per-agent prompt drift across 25 agents.

Input: { agentName: string, subagentType: string, inlineSkills: string[], promptBody: string }
Output: string — the final Agent prompt body with the skill-injection block prepended

Skill-injection block (canonical text):
  "MANDATORY — Before writing any code or invoking any tool that exercises the listed
  skills, invoke each of the following via the Skill tool (in order):
    Skill('skill-1')
    Skill('skill-2')
    ...

  Each skill must be invoked at least once before its first relevant action. Skills do
  NOT propagate across agents — if your team's other agents need the same skill, each
  invokes it independently."

Function signature:
  composePrompt(opts: {
    agentName: string,
    subagentType: string,
    inlineSkills: string[],
    promptBody: string
  }): string

Verifier check (TA1/TA2/TA3/TB1/TD1 verifiers): each spawned Agent's prompt MUST contain
  the canonical block (regex match on the leading "MANDATORY — Before writing any code"
  line). FAIL if missing.

Tests: scripts/__tests__/dispatch-helper.test.ts — fixture-based: input agent spec →
  expected prompt string; round-trip verifier-compatible.
```

**Owner:** Coordinator-side (Bond) — not assigned to any of the 5 teams. Lands as a single commit before Dispatch Wave 1: `feat(coordinator): dispatch-helper for skill-injection canonical text`.

**Cross-reference in spawn prompts:** Update Dispatch Rule 5 to reference `scripts/dispatch-helper.ts` as the canonical translator (not "Bond hand-edits each agent prompt").

### Group E — Rulings (David's answers)

| Ruling | Answer | Where it lands |
|---|---|---|
| R-v2.1.A | **Option C** — keep 5-7d target with explicit fail-forward on >200 LOC fs-refactors deferred to v2.2 | TA1.langgraph-wirer guardrails (per Issue 5 fix above); update Dispatch blockers §"Still open" — mark R-v2.1.A RESOLVED |
| R-v2.1.D | **Confirm** — `≤ $500/mo at 100 DAU` is v2.1 ship gate; AV.01 ($320/mo) carries to v2.2 | TB1.observability + TB1.verifier — pin EC-V21-B.6 to `≤ $500/mo` per load-test-tb1.ts projection; AV.01 alignment listed in `plans/post-v2.1-followups.md` for v2.2 (Wave E heuristic engine cost lever); reconcile internal $500 vs $644 drift in TB1 doc by deleting the $644 reference (line ≈949) and using $500 consistently; update Dispatch blockers §"Still open" — mark R-v2.1.D CONFIRMED |
| R-v2.1.B (chat-bridge in TA1 vs own team) | Bond default held: chat-bridge folded in TA1 | No edit needed |
| R-v2.1.C (sidecar + API routes together vs split) | Bond default held: together in TA3 | No edit needed |
| R-v2.1.E (METHODOLOGY-CORRECTION canonical) | RESOLVED — non-decision per Issue 1+2 fact-check; `plans/kb-upgrade-v2/` is the only path with all 8 modules | Per Issue 1+2+20 fix above; collapse from 5 R-rulings → 4 (then → 0 after applying A/D/E) |

---

## D-V21.24 spec (already added to master plan v2.1)

For reference — the row added to the Decisions table:

```
| **D-V21.24** | Synthesis pipeline split (Vercel ↔ Cloud Run boundary) | **Vercel hosts the LangGraph orchestration; Cloud Run sidecar = per-artifact rendering only.** Sidecar receives `POST /run-render` with `{project_id, artifact_kind, agent_output_payload}`; renders via canonical Python generators (`gen-arch-recommendation` / `gen-qfd` / `gen-fmea` / etc.); writes `project_artifacts` row. Cold-start budget applies only to render path; LLM cost stays on Vercel-side Anthropic SDK metering for unified Sentry instrumentation (TB1). | User-locked 2026-04-25 19:50 EDT per critique iter-1 Issue 11 (`team-spawn-prompts-v2.1-CRITIQUE.md`). Removes ambiguity at TA1↔TA3 boundary in spawn prompts. Honored by TA1 `langgraph-wirer` (Vercel-side) + TA3 `python-sidecar` (rendering-only). |
```

---

## Recommended action sequence for fresh Claude

```
1. READ this handoff + .claude/plans/team-spawn-prompts-v2.1.md + .claude/plans/team-spawn-prompts-v2.1-CRITIQUE.md
2. VERIFY plans/c1v-MIT-Crawley-Cornell.v2.1.md contains D-V21.24 (line ≈236)
3. APPLY Group A factual corrections to spawn-prompts file (~30 min, 7 edit clusters)
4. APPLY Group B architectural locks to spawn-prompts file (~45 min, 7 edit clusters incl. agent-disposition table)
5. APPLY Group E rulings to spawn-prompts file:
   - Update Dispatch blockers §"Still open" — strike R-v2.1.E (resolved); mark R-v2.1.A as Option C resolved + R-v2.1.D as confirmed; B and C as Bond-default-held
   - Reconcile cost math: delete $644 reference at line ≈949; pin EC-V21-B.6 to ≤ $500/mo consistently
6. BUILD scripts/dispatch-helper.ts per Group D spec (~1hr)
7. UPDATE Dispatch Rule 5 in spawn-prompts to reference scripts/dispatch-helper.ts as canonical translator
8. COMMIT each Group as its own commit:
   - feat(plans): D-V21.24 sidecar boundary lock (already done)
   - docs(spawn-v2.1): Group A factual corrections from critique iter-1
   - docs(spawn-v2.1): Group B architectural locks from critique iter-1
   - docs(spawn-v2.1): Group E David rulings + R-v2.1.E collapse
   - feat(coordinator): scripts/dispatch-helper.ts for skill-injection canonical text
9. SURFACE to David: spawn-prompts ready for critique iter 2 OR direct dispatch
```

**Estimated total time:** 2-3 hours mechanical work. Net reduces dispatch-time reconciliation across 25 agents from ~8-12 person-hours to ~0.

---

## Open follow-ups (post-fix-up; NOT in fix-up scope)

- **`apps/product-helper/CLAUDE.md` edits** (path-claim fix per Issue 2 + new Deployed Features entries from each team's docs agent) — surface diff in `plans/v21-outputs/<team>/claude-md-diff.md` for each team; ALL pending David authorization per file-safety rule. Do NOT edit CLAUDE.md directly.
- **`plans/post-v2.1-followups.md`** — create this file during the fix-up sweep with: (a) AV.01 alignment ($320/mo target via Wave E heuristic engine; carries to v2.2), (b) deferred fs-side-effects refactors > 200 LOC discovered by TA1.migrations-and-agent-audit, (c) TD1 fixture-vs-live preflight drift (if observed).
- **Critique iteration 2** — David may request another critique pass after the fix-up sweep lands. Run `/gsd-review` or equivalent against the post-fix-up spawn-prompts file.
- **Master plan v2.1 flip DRAFT → ready-for-execution** — Bond surfaces "spawn prompts ready" + David flips master plan status before Dispatch Wave 1.

---

## Cross-references

- Master plan: [`plans/c1v-MIT-Crawley-Cornell.v2.1.md`](c1v-MIT-Crawley-Cornell.v2.1.md) (D-V21.24 added)
- Spawn prompts (DRAFT): [`.claude/plans/team-spawn-prompts-v2.1.md`](../.claude/plans/team-spawn-prompts-v2.1.md)
- Critique iter-1: [`.claude/plans/team-spawn-prompts-v2.1-CRITIQUE.md`](../.claude/plans/team-spawn-prompts-v2.1-CRITIQUE.md)
- v2 spawn prompts (predecessor pattern): [`.claude/plans/team-spawn-prompts-v2.md`](../.claude/plans/team-spawn-prompts-v2.md)
- v2.2 stub (deferred work): [`plans/c1v-MIT-Crawley-Cornell.v2.2.md`](c1v-MIT-Crawley-Cornell.v2.2.md)

---

**END OF HANDOFF.** Fresh Claude: start at §"Recommended action sequence" line 1.
